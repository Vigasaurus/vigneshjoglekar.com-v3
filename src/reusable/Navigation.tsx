import React from 'react';

interface Props {}

const Navigation = (props: Props) => {
	return <React.Fragment />;
};

export default Navigation;

/*
	Navigation menu or system available on every page (back button is not sufficient)
*/
